//TODO: Infer BPM from mic samples
/*class BPMTool {
	constructor() {
		this._max = 0;
		this._bpm = undefined;
	}

	audioFrame(data) {
		const samples = 50;
		const stepSize = Math.ceil(data.length/samples);
		for(var i=0;i<data.length;i++) {
			this._max = Math.max(data[i*stepSize], this._max);


		}
	}
}*/